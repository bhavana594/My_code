#include<stdio.h>
bool isprime(int n)
{	int count=0;
	for(int i = 1; i <= n; i++)
	{
 		if(n%i==0)
 		count++;
	}
	if(count == 2)
	{
	 	return true;//return 1
	}
	else
	{
	 	return false;//return 0
	}
}
int main()
{
	
	int n;
	scanf("%d",&n);
	printf("%d",isprime(n));
	return 0;
}
