#include<stdio.h>
int sum(int n)
{
	if(n == 0 || n==1)
	{
		return 1;
	}
	else
	{
		return n+sum(n-1);
	}
}
int main()
{
	int n;
	printf("Enter a number:");
	scanf("%d",&n);
	printf("Fact of the given number is:%d",sum(n));
	return 0;
}
