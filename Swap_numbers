#include<stdio.h>
int main()
{
	int a,b,temp;
	scanf("%d %d",&a,&b);
	temp = a;
	a = b;
	b = temp;
	printf("%d %d",a,b);
}
//or we can also do like this
//a = a+b;
//b = a-b;
//a = a-b;
//or use * and / by  + -
// use xor
